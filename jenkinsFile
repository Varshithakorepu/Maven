pipeline {
    agent any

    tools {
        jdk 'JDK21'
    }

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Build & Test') {
            steps {
                bat 'mvn -B clean package'
            }
        }

        stage('Archive') {
            steps {
                // archive all jars inside any target folder
                archiveArtifacts artifacts: '**/target/*.jar', fingerprint: true
            }
        }
    }
}
